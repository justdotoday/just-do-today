<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.just_do_today.userCategory.mapper.CategoryUserMapper">

    <!-- 카테고리 관리 화면용: 유저 카테고리 목록 + 습관 수 -->
    <select id="findAllWithCountByMemberId"
            resultType="com.example.just_do_today.userCategory.dto.CategoryManageResDto"
            parameterType="long">
        SELECT
            cu.id        AS categoryUserId,
            cu.name      AS name,
            cu.emoji,
            COUNT(uh.id) AS habitCount
        FROM category_user cu
        LEFT JOIN user_habit uh ON uh.category_user_id = cu.id
        WHERE cu.member_id = #{memberId}
        GROUP BY cu.id, cu.name, cu.emoji
    </select>

</mapper>
